# The saved session and the bearer token lifted from it. Regenerated by the
# setup project on every run, and a real credential for whatever installation
# the suite last pointed at - so never committed.
.auth/

node_modules/
test-results/
playwright-report/
blob-report/
